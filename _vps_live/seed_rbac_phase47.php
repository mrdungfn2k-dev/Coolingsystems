<?php
$pdo=new PDO('sqlite:/var/lib/coolingsystems/cooling.db',null,null,[PDO::ATTR_ERRMODE=>PDO::ERRMODE_EXCEPTION]);$pdo->beginTransaction();try{$pdo->exec("CREATE TABLE IF NOT EXISTS product_serials (id INTEGER PRIMARY KEY AUTOINCREMENT,product_id INTEGER NOT NULL,serial_no TEXT NOT NULL UNIQUE,manufactured_at TEXT,warranty_end_date TEXT NOT NULL,created_by INTEGER,created_at TEXT NOT NULL DEFAULT (datetime('now','localtime')),FOREIGN KEY(product_id) REFERENCES products(id))");$pdo->prepare('INSERT OR REPLACE INTO rbac_capability_rules (capability,permission_code,allowed_levels) VALUES (?,?,?)')->execute(['catalog.serials.manage','P026',json_encode(['TQ','QL','TH'])]);$pdo->commit();echo '{"ok":true}';}catch(Throwable $e){if($pdo->inTransaction())$pdo->rollBack();throw $e;}
